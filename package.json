{
  "name": "peggy",
  "version": "1.0.2",
  "description": "Parser generator for JavaScript",
  "keywords": [
    "parser generator",
    "PEG",
    "PEG.js"
  ],
  "homepage": "https://peggyjs.org/",
  "repository": "peggyjs/peggy",
  "bugs": "https://github.com/peggyjs/peggy/issues",
  "license": "MIT",
  "author": "David Majda <david@majda.cz> (https://majda.cz/)",
  "main": "lib/peg.js",
  "unpkg": "browser/peggy.min.js",
  "jsdelivr": "browser/peggy.min.js",
  "types": "lib/peg.d.ts",
  "bin": "bin/peggy",
  "scripts": {
    "clean": "rimraf -f build && rimraf -f browser && mkdir build && cd build && mkdir ts && mkdir rollup && mkdir terser && cd .. && mkdir browser",
    "jest": "jest",
    "terser": "terser -c passes=2 -m --sequences 40 build/rollup/peg.umd.js > build/terser/peggy.umd.min.js && terser -c passes=2 -m --sequences 40 --module build/rollup/peg.esmodule.js > build/terser/peggy.esmodule.min.js",
    "rollup": "rollup -c",
    "ts": "tsc --build tsconfig.json",
    "set_version": "node -e \"const fs = require('fs'), pkg = JSON.parse(fs.readFileSync('./package.json')); fs.writeFileSync('./lib/version.js', '\\n\\'use strict\\';\\n\\nmodule.exports = \\'' + pkg.version + '\\';\\n');\"",
    "newbuild": "npm run clean && npm run set_version && npm run ts && npm run rollup && npm run terser && npm run jest",
    "test": "mocha --recursive test --exclude test/browser.stub.js --reporter min",
    "lint": "eslint . --ext .js",
    "benchmark": "./benchmark run",
    "build": "npm run browser:build && npm run benchmark:build && npm run test:build",
    "browser:build": "browserify lib/peg.js -s peggy -o browser/peggy.min.js",
    "postbrowser:build": "tools/header browser/* && cp browser/peggy.min.js docs/vendor/peggy/peggy.min.js",
    "benchmark:build": "browserify benchmark/browser.stub.js -o docs/js/benchmark-bundle.min.js && tools/header docs/js/benchmark-bundle.min.js",
    "test:build": "browserify test/browser.stub.js -o docs/js/test-bundle.min.js && tools/header docs/js/test-bundle.min.js",
    "parser": "node ./bin/peggy -o lib/parser.js --format commonjs src/parser.pegjs"
  },
  "devDependencies": {
    "chai": "4.3.4",
    "express": "4.17.1",
    "jest": "^26.6.3",
    "mocha": "^8.3.2",
    "morgan": "1.10.0",
    "nve": "^13.0.0",
    "open": "^8.0.6",
    "rimraf": "^3.0.2",
    "rollup": "^2.45.2",
    "rollup-plugin-commonjs": "^10.1.0",
    "rollup-plugin-node-resolve": "^5.2.0",
    "sinon": "10.0.0",
    "terser": "^5.6.1",
    "typescript": "^4.2.4"
  },
  "engines": {
    "node": ">=10"
  }
}
